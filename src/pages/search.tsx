import { Htag } from "@/components";
import { withLayout } from "@/layout/Layout";
import React from "react";

const Search = (): JSX.Element => {
    return <Htag tag="h1">search...</Htag>;
};

export default withLayout(Search);
